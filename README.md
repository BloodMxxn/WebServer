# Node.js Web Server

A lightweight web server built with **Node.js**.  

- 🚀 Minimal and easy to use
- ⚙️ Choose your own port with `-p` or `--port`

## Installation
Clone the repository and install Node.js if you haven't already.

```bash
git clone https://github.com/BloodMxxn/WebServer.git
cd WebServer
```
## Usage
Run the server with Node.js:

```bash
node server.js
```
By default, the server runs on port `80`.
You can specify a custom port:


```bash
node server.js -p 3000
```
